const overlayStub = {
  startOverlay: () => {},
  stopOverlay: () => {},
  toggleOverlay: () => {}
};
export const startOverlay = overlayStub.startOverlay;
export const stopOverlay = overlayStub.stopOverlay;
export const toggleOverlay = overlayStub.toggleOverlay;
export default overlayStub;
